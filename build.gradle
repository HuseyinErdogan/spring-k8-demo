buildscript {
    ext {
        rootProject.version = "0.18.0"
        project.version = "0.18.0"
        if (project.hasProperty('projVersion')) {
            project.version = project.projVersion
        }
        isProduct = 'true'
        publishSource = 'true'
    }
}